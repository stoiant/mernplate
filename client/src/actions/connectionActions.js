export const setUrl = (url) => {
  return {
    type: 'setUrl',
    url
  }
}

export const setNodeUrl = (nodeUrl) => {
  return {
    type: 'setNodeUrl',
    nodeUrl
  }
}

export const setLogUrl = (logUrl) => {
  return {
    type: 'setLogUrl',
    logUrl
  }
}

export const setNotifications = (notification) => {
  return {
    type: 'setNotifications',
    notification
  }
}

export const setTheme = (theme) => {
  return {
    type: 'setTheme',
    theme
  }
}
